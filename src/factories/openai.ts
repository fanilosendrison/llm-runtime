// NIB-M-FACTORIES — createOpenAIAdapter. Stub only.

import type { AdapterConfig, ProviderAdapter } from '../types.js';

export function createOpenAIAdapter(_config: AdapterConfig): ProviderAdapter {
  throw new Error('Not implemented');
}
