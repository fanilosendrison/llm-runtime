// NIB-M-FACTORIES — createAnthropicAdapter. Stub only.

import type { AdapterConfig, ProviderAdapter } from '../types.js';

export function createAnthropicAdapter(_config: AdapterConfig): ProviderAdapter {
  throw new Error('Not implemented');
}
